/**
 * VoiceButton — désactivé sur le site public tant que le flux Realtime
 * n'est pas relayé côté serveur avec une authentification éphémère.
 *
 * Ne jamais exposer une clé d'agent dans le bundle navigateur.
 */
export default function VoiceButton() {
  return null;
}
